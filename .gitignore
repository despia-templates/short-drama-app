node_modules/
dist/
deploy/
server/generated/
.env.local
.dev-session.json
.agents/
.claude/skills/
public/posters/*.png
public/assets/*.png

# the demo HLS ladders + subtitle renditions are generated from the MP4 sources (scripts/gen-hls.mjs)
public/media/*/

# a local, unsigned export entitlement (docs/monetization.md §7) — never committed; a release needs a signed one
despia-entitlement.json
