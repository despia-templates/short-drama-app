{
  "_note": "NATIVE IDENTITY + ORIGIN. The web lane never needs this file: it is same-origin by construction. The native lane has no origin of its own, so `host` is what a root-relative <api url=\"/x\"> and every <image src=\"/posters/\u2026\"> resolve against. Set it to YOUR deployed https origin before building the native lane. It is deliberately not `localhost`: a device cannot reach your laptop by that name, and the export ships no assets of its own (upstream 279), so the origin serves the art as well as the data. To test on a simulator or a device against a local server, use your machine's LAN address \u2014 `http://192.168.x.x:8787` \u2014 and note that cleartext needs an ATS exception the generated Info.plist does not declare. See README, 'The native lane'.",
  "name": "Short Drama",
  "host": "https://shortdrama.example.com",
  "entry": {
    "root": "/",
    "surfaces": [
      "shortdrama.App"
    ]
  },
  "icons": "unified",
  "design": "custom",
  "_consts_note": "THE DEPLOYMENT PLANE. `consts` is App.json's flat-scalar block, read in markup as `dsx.const.<key>` (reactive, and typed-NULL when a key is absent — which is exactly the gate an honest degradation needs). A local build may override any of these in dsx.config.json `consts` without editing the file that ships. Everything below is EMPTY ON PURPOSE: a placeholder URL looks satisfied to an adopter and reads as a broken promise to a store reviewer. See docs/auth.md and Components/parts/AuthSeam.dsx.",
  "consts": {
    "authSessionUrl": "/dev-session.json",
    "authSignInUrl": "",
    "authSignUpUrl": "",
    "authSignOutUrl": "",
    "accountDeleteUrl": "/viewer/delete",
    "authDeviceRegisterUrl": "/auth/device/register",
    "authDeviceUrl": "/auth/device",
    "authLinkUrl": "/auth/link",
    "authUnlinkUrl": "/auth/unlink",
    "followFacebookUrl": "",
    "followYoutubeUrl": "",
    "followTiktokUrl": "",
    "followInstagramUrl": "",
    "termsUrl": "",
    "privacyUrl": "",
    "supportUrl": ""
  }
}