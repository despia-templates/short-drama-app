{
  "name": "Short Drama",
  "entry": "App",
  "outDir": "dist",
  "routes": [
    {
      "path": "/",
      "tabRoot": true,
      "component": "shortdrama.App",
      "meta": {
        "title": "Short Drama — binge-worthy vertical series"
      },
      "motion": {
        "mobile": "none",
        "wide": "dsx"
      }
    },
    {
      "path": "/discover",
      "tabRoot": true,
      "component": "shortdrama.Discover",
      "meta": {
        "title": "Discover — Short Drama"
      },
      "motion": {
        "mobile": "none",
        "wide": "dsx"
      }
    },
    {
      "path": "/vip",
      "component": "shortdrama.Vip",
      "meta": {
        "title": "VIP — Short Drama"
      },
      "_note": "NOT A TAB ROOT ANY MORE (docs/design/reference-ui-spec.md §8): the five-tab bar is Home · Explore · My List · Rewards · My Page, and VIP lives in the crown button, the unlock pills and the Membership page. /vip stays a route — the crown's destination until /membership carries every plan (Components/parts/Theme.dsx membershipPath) — and it is PUSHED like every other non-tab page, so no `motion` key here either."
    },
    {
      "path": "/store",
      "component": "shortdrama.Store",
      "meta": {
        "title": "Store — Short Drama"
      }
    },
    {
      "path": "/membership",
      "component": "shortdrama.Membership",
      "meta": {
        "title": "Membership — Short Drama"
      },
      "_note": "PUSHED on every width (no `motion` key, the same reasoning /store carries): the crown, the player's unlock pill and every 'see plans' door push it, and it goes back with a back chevron. The plans it sells are the SERVER's catalogue (server/store.dsx storeCatalog); the purchase is Core/Store through the RevenueCat provider on a device and Stripe on the web (Components/parts/BuyButton.dsx)."
    },
    {
      "path": "/rewards",
      "tabRoot": true,
      "component": "shortdrama.Rewards",
      "meta": {
        "title": "Rewards — Short Drama"
      },
      "motion": {
        "mobile": "none",
        "wide": "dsx"
      },
      "_note": "A TAB ROOT now (docs/design/reference-ui-spec.md 8: the fourth tab). `motion: {mobile: none}` is the tab-root declaration — a tab switch is a reset, not a push, so the screen swaps in place and the tab bar never rides a transition (PLAN.md 6.29). Every door that used to PUSH /rewards resets to it now, for the same reason."
    },
    {
      "path": "/list",
      "tabRoot": true,
      "component": "shortdrama.MyList",
      "meta": {
        "title": "My List — Short Drama"
      },
      "motion": {
        "mobile": "none",
        "wide": "dsx"
      }
    },
    {
      "path": "/profile",
      "tabRoot": true,
      "component": "shortdrama.MyPage",
      "meta": {
        "title": "My Page — Short Drama"
      },
      "motion": {
        "mobile": "none",
        "wide": "dsx"
      },
      "_note": "AN ALIAS of /me, kept so every link, bookmark and TopNav/PersonalNav path that says /profile still lands on the same screen. Both routes are tab roots and render Components/MyPage.dsx — the Profile screen restyled to the reference (spec 8 'My Page'). Components/Profile.dsx is no longer routed."
    },
    {
      "path": "/me",
      "tabRoot": true,
      "component": "shortdrama.MyPage",
      "meta": {
        "title": "My Page — Short Drama"
      },
      "motion": {
        "mobile": "none",
        "wide": "dsx"
      },
      "_note": "THE FIFTH TAB (docs/design/reference-ui-spec.md 8, 'My Page'): Components/MyPage.dsx, the Profile screen restyled to the reference with every row it had. /profile aliases it."
    },
    {
      "path": "/notices",
      "component": "shortdrama.Notices",
      "meta": {
        "title": "Notifications — Short Drama"
      }
    },
    {
      "path": "/account",
      "component": "shortdrama.Account",
      "meta": {
        "title": "Account — Short Drama"
      }
    },
    {
      "path": "/auth/:pane",
      "component": "shortdrama.Auth",
      "meta": {
        "title": "Account — Short Drama"
      }
    },
    {
      "path": "/browse/:genre",
      "component": "shortdrama.Browse",
      "meta": {
        "title": "Browse — Short Drama"
      }
    },
    {
      "path": "/browse",
      "component": "shortdrama.Browse",
      "meta": {
        "title": "Browse — Short Drama"
      }
    },
    {
      "path": "/show/:id",
      "component": "shortdrama.Show",
      "meta": {
        "title": "Show — Short Drama"
      }
    },
    {
      "path": "/watch/:show/:idx",
      "component": "shortdrama.Watch",
      "meta": {
        "title": "Watch — Short Drama"
      }
    },
    {
      "path": "/invite/:code",
      "component": "shortdrama.Invite",
      "meta": {
        "title": "You are invited — Short Drama"
      },
      "_note": "THE INVITE LINK (docs/design/reference-ui-spec.md 9.3 row 5): a friend's share carries /invite/<code>; the screen keeps the code in a cookie and replaces itself with Home, where Components/parts/ReferralSeam.dsx attaches it to the device identity. The inviter is credited only when this device records its first server-verified watch minute (server/engage.dsx)."
    },
    {
      "path": "/admin",
      "component": "shortdrama.Admin",
      "meta": {
        "title": "Manage — Short Drama"
      }
    }
  ],
  "_consts_note": "THE FOLLOW ROWS (docs/design/reference-ui-spec.md 9.3 row 6) read their destinations from `dsx.const.follow*Url`, which App.json ships EMPTY on purpose — a placeholder URL reads as a broken promise to a store reviewer — and a row whose URL is empty is not rendered. This local overlay points the demo at the networks' own front pages so the page can be judged against the reference; an adopter puts their real pages in App.json.",
  "consts": {},
  "router": {
    "transition": "ios",
    "swipeBack": true,
    "motionBreakpoint": 768,
    "wide": "dsx"
  },
  "_packages_note": "THE IN-APP-PURCHASE LANE IS HERE: Core/Store (scheme `store`) and Core/RevenueCat (scheme `revenuecat`, the provider the Store module cross-calls for `provider: \"revenuecat\"`). App Store 3.1.1 names in-game currencies and premium-content unlocks as in-app-purchase products, so coins and the VIP pass are sold through StoreKit and Play Billing inside a native build and through Stripe on the web storefront only; Components/parts/BuyButton.dsx picks the lane by capability (`has('store')` first, `os == 'web'` second) and the server grants on both (server/store.dsx: POST /store/native, the RevenueCat <webhook> and one shared granter). WHAT AN ADOPTER CONFIGURES is one page, docs/monetization.md: the RevenueCat public SDK keys, the two server secrets, the product ids on the catalogue rows, and the StoreKit configuration file for the simulator. The native exports read `Modules/` and the lockfile today, so a package declared here by path reaches the WEB build and not yet a device build (PLAN.md 6.115 records the upstream ask; an engine agent is landing the fold). Until it lands `has('store')` is false on a device and the purchase control shows one line — 'In-app purchases are not available in this build' — and charges nothing.",
  "packages": [
    "../despia_dsx/despia-framework/ClosedSource/DSX/Modules/Core/Payments/Stripe",
    "../despia_dsx/despia-framework/ClosedSource/DSX/Modules/Core/SocialShare",
    "../despia_dsx/despia-framework/ClosedSource/DSX/Modules/Core/PostHog",
    "../despia_dsx/despia-framework/ClosedSource/DSX/Modules/Core/Consent",
    "../despia_dsx/despia-framework/ClosedSource/DSX/Modules/Core/Store",
    "../despia_dsx/despia-framework/ClosedSource/DSX/Modules/Core/RevenueCat",
    "../despia_dsx/despia-framework/ClosedSource/DSX/Modules/Core/LocalPush",
    "../despia_dsx/despia-framework/ClosedSource/DSX/Modules/Core/Notify",
    "../despia_dsx/despia-framework/ClosedSource/DSX/Modules/Core/IdentityVault",
    "../despia_dsx/despia-framework/ClosedSource/DSX/Modules/Core/Extensions/ActivityKit",
    "../despia_dsx/despia-framework/ClosedSource/DSX/Modules/Core/Widgets"
  ],
  "_remind_note": "REMIND ME (docs/design/reference-ui-spec.md §2–§2b) is two modules. Core/Notify (scheme `notify`) owns the PERMISSION: `dsx.module.notify.permission()` is the one ladder on three renderers (the kernel's NotifyCore, corpus-pinned), and it is what the permission sheet's 'Allow notifications' asks. Core/LocalPush (scheme `localpush`) owns the SCHEDULE: `send({ seconds, id, title, message, url })` is a one-shot the OS fires at the premiere, after force-quit and reboot, on iOS and Android. The web facet of `send` is a page-side timer that dies with the tab — honest for a delay of minutes, a lie for a premiere in nine days — so Components/App.dsx does not arm it there and says so in the sheet; the server row (server/reminders.dsx) is what survives on every lane. Both packages reach the web build from this list; the native exports read `Modules/` and the lockfile (PLAN.md 6.115), so on a device `has('localpush')` is false until that fold lands and the sheet names the gap instead of pretending.",
  "_modules_note": "DECLARE-THEN-REACH. `modules` is the LINTER's allowlist of platform module schemes this app reaches and no configured `package` claims. `posthog` and `consent` are for Components/parts/Analytics.dsx: Core/PostHog is the framework's product-analytics verb and Core/Consent its gate. They used to be deliberately ABSENT from `packages`, because both shipped a complete web/index.js that no build could load — a browser chunk was emitted only for a manifest that restated `web.entry`, ten modules did, and 98 did not. That was PLAN.md §6.92 and it is fixed upstream: file presence is the gate now (`webLaneEntry`), so declaring them here emits real chunks and has('posthog') can finally answer true. The guards stay on every call regardless — a build that excludes them must still run.",
  "modules": [
    "stripe",
    "share",
    "posthog",
    "consent",
    "store",
    "localpush",
    "notify",
    "identityvault"
  ],
  "_surfaces_note": "THE SURFACES OUTSIDE THE APP. Core/Extensions/ActivityKit (scheme liveactivity) and Core/Widgets (scheme widget) are the framework's Live Activity and home-screen widget lanes; Components/parts/NowPlayingSurface.dsx publishes the 'Currently Watching' card through them (spec §7) and Components/surfaces/NowPlaying{Activity,Widget}.dsx are the snapshot documents they render — first-class .dsx files, lint-checked, shipped byte-exact by both native exports and named by NAME in the module calls. Neither module has a web lane (a browser owns no lock screen), so this list admits the calls to the linter and the browser build emits no chunk for them; the native exports synthesize the ActivityKitExtension + ImageWidgetExtension targets, the App Group entitlement and the Glance receiver from the same two manifests (PLAN.md 6.153). They are listed here beside the packages note's caveat: until the native exports read this list (6.115), a device build reaches them through a Modules/ mount."
}
